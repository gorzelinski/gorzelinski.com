import styled from "styled-components"

export const Avatar = styled.div`
  display: flex;
  gap: var(--space-s);
  align-items: center;

  .gatsby-image-wrapper {
    border-radius: 50%;
    flex-shrink: 0;
  }
`
